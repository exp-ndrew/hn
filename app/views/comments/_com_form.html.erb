<%= render('layouts/errors', :object => @comment) %>

<%= form_for([@commentable, @comment]) do |f| %>

    <div class="form-group">
      <%= f.label :text %>
      <p> hey this is the com_ </p>
      <%= f.text_field :text, :class => "form-control" %>
    </div>
    <%= f.submit(:class => "btn btn-primary") %>
<% end %>
